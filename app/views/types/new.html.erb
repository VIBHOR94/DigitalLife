<h1>Add Type Detail</h1>
<%= form_for @type do |f|%> 
	<%if @type.errors.any? %>
		<h4>These erros prvented the form from being saved.</h4>
		<ul style="color:red">
			<% @type.errors.full_messages.each do |msg| %>
				<li><%=msg %></li>
			<%end%>
		</ul>
	<%end%>  

	<%="Name : "%><%=f.text_field :name%><br />
	<%=f.submit%>
<%end%>
